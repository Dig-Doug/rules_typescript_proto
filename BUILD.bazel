load("@npm_bazel_typescript//:index.bzl", "ts_config")

package(default_visibility = ["//:__subpackages__"])

exports_files([
    "tsconfig.json",
])

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
    deps = [],
)
